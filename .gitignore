# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environments
.env
.env.local
.env.*.local
backend/.env
frontend/.env

node_modules
dist
dist-ssr
*.local

# Test coverage artifacts (generados; no versionar)
coverage
**/coverage

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
supabase/.temp/
supabase.tar.gz
test-ingest.mjs

# Private Documentation
TECHNICAL_DOCUMENTATION.md
DEPLOYMENT.md

# Guía local para Claude Code (no versionar)
CLAUDE.md

# Memoria de trabajo local del agente (no versionar)
memory/

# Configuración local de Claude Code. Hasta ahora solo la ignoraba la config
# personal de git (~/.config/git/ignore), que no viaja con el repo: en un clon
# nuevo estos archivos aparecían sin seguimiento, listos para un `git add .`.
# settings.json (compartido) sí puede versionarse.
.claude/settings.local.json
.claude.json
.mcp.json

# Generated reports (don't count towards GitHub language stats)
playwright-report/
test-results/
backend/coverage/
