/**
 * frontend/src/services/index.ts
 *
 * Barrel export for all services.
 * Components import like: import { submitContactMessage } from '@/services'
 */

export { submitContactMessage } from './contact.service';
export { registerForEvent } from './events.service';
